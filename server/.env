PORT=5000
MONGODB_URI=mongodb://localhost:27017/tutorial_app
JWT_SECRET=your_jwt_secret_key_here
NODE_ENV=development

# Fallback for when local MongoDB is not available
# If local fails, we'll create in-memory mock data